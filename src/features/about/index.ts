/**
 * MOD-about Public API — PRD_TECHNICAL §7.9
 *
 * 현재(M1): AboutProfile 뼈대.
 *
 * 향후 (PRD §7.9 계약):
 * - M4-20: getAboutContent — contents/about/index.md를 MdxRemote로 렌더.
 *
 * 규칙: 다른 feature를 import하지 않는다 (Law 3).
 */

export { AboutProfile, HomeHero } from "./components";
