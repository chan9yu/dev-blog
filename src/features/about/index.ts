/**
 * MOD-about Public API — PRD_TECHNICAL §7.9
 *
 * 현재(M1): 뼈대. 공개 타입 없음.
 *
 * 향후 추가 (PRD §7.9 계약):
 * - 컴포넌트 (M1-50): AboutProfile
 * - 서비스 (M4-20): getAboutContent (contents/about/index.md를 MdxRemote로 렌더)
 *
 * 규칙: 다른 feature를 import하지 않는다 (Law 3).
 */

export {};
