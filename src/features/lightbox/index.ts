/**
 * MOD-lightbox Public API — PRD_TECHNICAL §7.8
 *
 * 현재(M1): 뼈대. 공개 타입 없음.
 *
 * 향후 추가 (PRD §7.8 계약):
 * - 컴포넌트 (M1-57, M3-15~M3-16): ImageLightbox, LightboxProvider
 * - 훅: useLightbox, useLightboxContext
 *
 * 연동: shared/components/mdx/MdxImage와 페이지 단위 LightboxProvider가 루트에 위치.
 * 규칙: 다른 feature를 import하지 않는다 (Law 3).
 */

export {};
