/**
 * MOD-comments Public API — PRD_TECHNICAL §7.6
 *
 * 현재(M1): 뼈대. 공개 타입 없음.
 *
 * 향후 추가 (PRD §7.6 계약):
 * - 컴포넌트 (M1-58, M3-11~M3-12): CommentsSection (Giscus lazy-mount)
 *
 * 환경변수 의존: NEXT_PUBLIC_GISCUS_REPO, REPO_ID, CATEGORY, CATEGORY_ID (빌드 타임 주입).
 * 규칙: 다른 feature를 import하지 않는다 (Law 3).
 */

export {};
